# Copy this file to .env and fill in real values. .env is gitignored and
# must never be committed.

APP_NAME=mini-ats-backend
ENVIRONMENT=development

# --- Added in step 2 (JWT verification against Supabase) ---
# SUPABASE_URL=
# SUPABASE_JWKS_URL=

# --- Added in step 3+ (admin account creation, DB writes bypassing RLS) ---
# SUPABASE_SECRET_KEY=
